# Turbopack resolves `next/package.json` on disk under the project root; Yarn PnP has no
# `node_modules/next`, which breaks `next dev` with Turbopack. Use a classic install layout.
nodeLinker: node-modules
